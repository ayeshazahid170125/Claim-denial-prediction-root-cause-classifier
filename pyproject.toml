[project]
name = "claim-denial-prediction"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "pandas",
    "numpy",
    "scipy",
    "scikit-learn",
    "joblib",
    "lightgbm",
    "shap>=0.45.0",
    "fastapi",
    "uvicorn",
    "pydantic",
]

[tool.vercel]
entrypoint = "app.step12_fastapi:app"
