[project]
name = "claim-denial-prediction"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "joblib",
    "scikit-learn",
    "lightgbm",
    "pandas",
    "numpy",
    "shap",
]

[tool.vercel]
entrypoint = "app.step12_fastapi:app"
